# secrets — never commit
.env
.env.local
.worktrees/
.local/etsy-token.json

# raw provider exports can contain emails, shop domains, or other private fields
artifacts/merchgrid/sources/raw/

# node
node_modules/
dist/
*.tsbuildinfo
test-output.txt
